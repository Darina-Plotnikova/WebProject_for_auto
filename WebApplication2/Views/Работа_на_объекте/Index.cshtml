@model IEnumerable<WebApplication2.Models.Работа_на_объекте>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Выполняемая_работа)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Объект.Наименование_заказа)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Сотрудник.ФИО)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Выполняемая_работа)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Объект.Наименование_заказа)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Сотрудник.ФИО)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
